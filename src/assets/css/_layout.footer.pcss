.footer {
  padding-top: var(--spacing-3);
  padding-bottom: var(--spacing-2);
  border-top: 1px solid var(--color-gray-light);

  &__icon {
    color: var(--color-primary);
    display: inline-block;
    padding: 0 var(--spacing-n3);
  }

  &__credits {
    padding-bottom: var(--spacing-1);
  }
}
